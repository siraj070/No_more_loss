name: mvc
description: A new Flutter project for near expiry marketplace.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  provider: ^6.1.1
  google_fonts: ^6.1.0
  intl: ^0.19.0
  geolocator: ^13.0.1      # For location
  geocoding: ^3.0.0        # For address from coordinates
  image_picker: ^0.8.7+4
  firebase_storage: ^12.4.10
  file_picker: ^6.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
